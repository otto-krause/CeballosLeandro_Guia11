#include <stdio.h>
void kcuc (int a, int b)//Solo muestra valor.
{ 
    printf("Los nros son:\n%i \n%i \n", a,b); 
    }
int main ()
{ 
    int c,d; 
scanf("%i %i", &c, &d); 
kcuc(c,d); 
return 0; 
}
